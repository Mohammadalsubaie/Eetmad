#!/usr/bin/env sh

echo ""
echo "🔍 Running design validation before commit..."
echo ""

# Colors for output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Get staged .ts and .tsx files (from root)
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E 'frontend/eetmad/.*\.(tsx?|ts)$' || echo "")

# Navigate to frontend/eetmad directory
cd frontend/eetmad || exit 1

# Strip the 'frontend/eetmad/' prefix from file paths
STAGED_FILES=$(echo "$STAGED_FILES" | sed 's|^frontend/eetmad/||g')

if [ -z "$STAGED_FILES" ]; then
  echo "${YELLOW}⚠️  No TypeScript/TSX files staged${NC}"
  echo ""
  exit 0
fi

echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo "${BLUE}📋 Validating Staged Files${NC}"
echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""
echo "Files to validate:"
echo "$STAGED_FILES" | sed 's/^/  ✓ /'
echo ""

# Run design rules validation on staged files
echo "${BLUE}🔍 Running design rules validation...${NC}"
echo ""

if npm run validate:design $STAGED_FILES; then
  echo ""
  echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
  echo "${GREEN}✅ All staged files pass design rules validation!${NC}"
  echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
  echo ""
  exit 0
else
  echo ""
  echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
  echo "${RED}❌ Design rules validation failed!${NC}"
  echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
  echo ""
  echo "${YELLOW}💡 Quick fixes:${NC}"
  echo "  • Check: ${BLUE}scripts/quick-reference/common-fixes.md${NC}"
  echo "  • Interactive help: ${BLUE}npm run scripts:menu${NC}"
  echo "  • Review errors above and fix them"
  echo ""
  echo "${YELLOW}To commit anyway (not recommended):${NC}"
  echo "  ${BLUE}git commit --no-verify${NC}"
  echo ""
  echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
  echo ""
  exit 1
fi

