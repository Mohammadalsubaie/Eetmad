# تقرير مقارنة Actions: Frontend vs Endpoints

## Actions Comparison Report with Critical Warnings

**تاريخ التقرير:** 2025-01-27  
**الهدف:** مقارنة دقيقة لكل Action مع تحذيرات خطيرة للنواقص

---

## 🚨 ملخص تنفيذي - تحذيرات خطيرة

### النتيجة الإجمالية: **38%** من Actions مطورة في Frontend

| الوحدة         | المطلوب | المطور | النسبة    | حالة             |
| -------------- | ------- | ------ | --------- | ---------------- |
| Authentication | 20      | 7      | 35%       | ⚠️ **ناقص خطير** |
| Users          | 15      | 3      | 20%       | ⚠️ **ناقص خطير** |
| Suppliers      | 20      | 7      | 35%       | ⚠️ **ناقص خطير** |
| Categories     | 12      | 4      | 33%       | ⚠️ **ناقص خطير** |
| Requests       | 20      | 7      | 35%       | ⚠️ **ناقص خطير** |
| Offers         | 15      | 7      | 47%       | ⚠️ **ناقص خطير** |
| Projects       | 15      | 3      | 20%       | ⚠️ **ناقص خطير** |
| Contracts      | 12      | 3      | 25%       | ⚠️ **ناقص خطير** |
| Milestones     | 12      | 1      | 8%        | 🚨 **ناقص جداً** |
| Payments       | 15      | 5      | 33%       | ⚠️ **ناقص خطير** |
| Reviews        | 15      | 4      | 27%       | ⚠️ **ناقص خطير** |
| Messages       | 12      | 3      | 25%       | ⚠️ **ناقص خطير** |
| Notifications  | 10      | 4      | 40%       | ⚠️ **ناقص خطير** |
| Disputes       | 12      | 3      | 25%       | ⚠️ **ناقص خطير** |
| **المجموع**    | **200** | **61** | **30.5%** | 🚨 **ناقص خطير** |

---

## 🔴 تحذيرات خطيرة (Critical Warnings)

### ⚠️ **تحذير عام:**

**نسبة التطبيق 30.5% فقط!** هذا يعني أن **69.5%** من الـ Actions المطلوبة غير موجودة في Frontend.

**النتيجة:** Frontend **لا يمكنه العمل** بدون هذه الـ Actions. يجب إضافة **139 Action** على الأقل.

---

## 1️⃣ Authentication & Authorization

### المطلوب (20 endpoints):

#### ✅ المطور (7):

1. ✅ `POST /auth/login` → `loginUser()`
2. ✅ `POST /auth/register` → `registerUser()`
3. ✅ `POST /auth/logout` → `logoutUser()`
4. ✅ `POST /auth/refresh-token` → `refreshToken()`
5. ✅ `POST /auth/verify-email` → `verifyEmail()`
6. ✅ `POST /auth/forgot-password` → `forgotPassword()`
7. ✅ `POST /auth/reset-password` → `resetPassword()`

#### 🚨 الناقص (13) - **يجب إضافتها فوراً:**

| Endpoint                         | الوصف                        | الأهمية       | السبب                                        |
| -------------------------------- | ---------------------------- | ------------- | -------------------------------------------- |
| `POST /auth/change-password`     | تغيير كلمة المرور            | 🔴 **عالية**  | **مطلوب** - المستخدم يحتاج تغيير كلمة المرور |
| `POST /auth/verify-phone`        | التحقق من الجوال             | 🔴 **عالية**  | **مطلوب** - التحقق من رقم الجوال             |
| `POST /auth/resend-verification` | إعادة إرسال رمز التحقق       | 🟡 **متوسطة** | **مطلوب** - UX مهم                           |
| `POST /auth/logout-all`          | تسجيل الخروج من جميع الأجهزة | 🟡 **متوسطة** | **مطلوب** - أمان                             |
| `POST /auth/2fa/enable`          | تفعيل المصادقة الثنائية      | 🟡 **متوسطة** | **مطلوب** - أمان                             |
| `POST /auth/2fa/disable`         | تعطيل المصادقة الثنائية      | 🟡 **متوسطة** | **مطلوب** - أمان                             |
| `POST /auth/2fa/verify`          | التحقق من رمز 2FA            | 🟡 **متوسطة** | **مطلوب** - أمان                             |
| `GET /auth/2fa/backup-codes`     | إنشاء رموز احتياطية          | 🟡 **متوسطة** | **مطلوب** - أمان                             |

**النتيجة:** ⚠️ **65% ناقص** - يجب إضافة 13 Action

---

## 2️⃣ Users

### المطلوب (15 endpoints):

#### ✅ المطور (3):

1. ✅ `GET /users/me` → `getProfile()`
2. ✅ `PUT /users/me` → `updateProfile()`
3. ✅ `PUT /users/me/password` → `changePassword()` (⚠️ لكن endpoint مختلف)

#### 🚨 الناقص (12) - **يجب إضافتها فوراً:**

| Endpoint                                 | الوصف                | الأهمية       | السبب                                  |
| ---------------------------------------- | -------------------- | ------------- | -------------------------------------- |
| `DELETE /users/me`                       | حذف الحساب           | 🔴 **عالية**  | **مطلوب** - GDPR compliance            |
| `PATCH /users/me/deactivate`             | تعطيل الحساب         | 🔴 **عالية**  | **مطلوب** - المستخدم يحتاج تعطيل حسابه |
| `PATCH /users/me/reactivate`             | إعادة تفعيل الحساب   | 🔴 **عالية**  | **مطلوب** - إعادة تفعيل                |
| `POST /users/me/avatar`                  | رفع صورة شخصية       | 🔴 **عالية**  | **مطلوب** - رفع الصورة الشخصية         |
| `DELETE /users/me/avatar`                | حذف صورة شخصية       | 🟡 **متوسطة** | **مطلوب** - حذف الصورة                 |
| `PUT /users/me/address`                  | تحديث العنوان        | 🔴 **عالية**  | **مطلوب** - تحديث العنوان              |
| `PUT /users/me/notification-preferences` | تفضيلات الإشعارات    | 🔴 **عالية**  | **مطلوب** - إدارة الإشعارات            |
| `GET /users/me/statistics`               | إحصائياتي            | 🟡 **متوسطة** | **مطلوب** - Dashboard                  |
| `GET /users/me/wallet/balance`           | رصيد المحفظة         | 🔴 **عالية**  | **مطلوب** - عرض الرصيد                 |
| `GET /users/{id}`                        | عرض ملف مستخدم عام   | 🟡 **متوسطة** | **مطلوب** - عرض الملف العام            |
| `GET /users/{id}/public-profile`         | الملف العام للمستخدم | 🟡 **متوسطة** | **مطلوب** - الملف العام                |

**النتيجة:** ⚠️ **80% ناقص** - يجب إضافة 12 Action

---

## 3️⃣ Suppliers

### المطلوب (20 endpoints):

#### ✅ المطور (7):

1. ✅ `GET /suppliers` → `getAll()`
2. ✅ `GET /suppliers/{id}` → `getById()`
3. ✅ `GET /suppliers/me` → `getMyProfile()`
4. ✅ `PUT /suppliers/me` → `updateProfile()`
5. ✅ `GET /suppliers/me/statistics` → `getStatistics()`
6. ✅ `POST /suppliers/me/portfolio` → `addPortfolioItem()`
7. ✅ `PUT /suppliers/me/portfolio/{id}` → `updatePortfolioItem()`
8. ✅ `DELETE /suppliers/me/portfolio/{id}` → `deletePortfolioItem()`

#### 🚨 الناقص (12) - **يجب إضافتها فوراً:**

| Endpoint                                   | الوصف                 | الأهمية       | السبب                                    |
| ------------------------------------------ | --------------------- | ------------- | ---------------------------------------- |
| `POST /suppliers/profile`                  | إنشاء ملف مورد        | 🔴 **عالية**  | **مطلوب** - إنشاء ملف المورد بعد التسجيل |
| `DELETE /suppliers/me`                     | حذف ملف المورد        | 🔴 **عالية**  | **مطلوب** - حذف الملف                    |
| `POST /suppliers/me/certifications`        | إضافة شهادة           | 🔴 **عالية**  | **مطلوب** - إضافة الشهادات               |
| `DELETE /suppliers/me/certifications/{id}` | حذف شهادة             | 🟡 **متوسطة** | **مطلوب** - حذف الشهادات                 |
| `PUT /suppliers/me/working-hours`          | تحديث ساعات العمل     | 🔴 **عالية**  | **مطلوب** - تحديث ساعات العمل            |
| `POST /suppliers/me/verification/request`  | طلب التوثيق           | 🔴 **عالية**  | **مطلوب** - طلب التوثيق                  |
| `DELETE /suppliers/me/verification/cancel` | إلغاء طلب التوثيق     | 🟡 **متوسطة** | **مطلوب** - إلغاء الطلب                  |
| `GET /suppliers/me/earnings`               | أرباحي                | 🔴 **عالية**  | **مطلوب** - عرض الأرباح                  |
| `GET /suppliers/me/performance`            | مؤشرات الأداء         | 🟡 **متوسطة** | **مطلوب** - Dashboard                    |
| `GET /suppliers/{id}/reviews`              | تقييمات المورد        | 🔴 **عالية**  | **مطلوب** - عرض التقييمات                |
| `GET /suppliers/top-rated`                 | أعلى الموردين تقييماً | 🟡 **متوسطة** | **مطلوب** - الصفحة الرئيسية              |
| `GET /suppliers/category/{categoryId}`     | موردين حسب الفئة      | 🟡 **متوسطة** | **مطلوب** - البحث                        |

**النتيجة:** ⚠️ **60% ناقص** - يجب إضافة 12 Action

---

## 4️⃣ Categories

### المطلوب (12 endpoints):

#### ✅ المطور (4):

1. ✅ `GET /categories` → `getAll()`
2. ✅ `GET /categories/{id}` → `getById()`
3. ✅ `POST /categories` → `create()` (Admin)
4. ✅ `PUT /categories/{id}` → `update()` (Admin)

#### 🚨 الناقص (8) - **يجب إضافتها فوراً:**

| Endpoint                             | الوصف              | الأهمية       | السبب                             |
| ------------------------------------ | ------------------ | ------------- | --------------------------------- |
| `GET /categories/tree`               | شجرة الفئات        | 🔴 **عالية**  | **مطلوب** - عرض الفئات بشكل هرمي  |
| `GET /categories/active`             | الفئات النشطة      | 🔴 **عالية**  | **مطلوب** - عرض الفئات النشطة فقط |
| `GET /categories/{id}/subcategories` | الفئات الفرعية     | 🔴 **عالية**  | **مطلوب** - عرض الفئات الفرعية    |
| `GET /categories/parent`             | الفئات الرئيسية    | 🟡 **متوسطة** | **مطلوب** - عرض الفئات الرئيسية   |
| `DELETE /categories/{id}`            | حذف فئة            | 🟡 **متوسطة** | **مطلوب** - Admin                 |
| `PATCH /categories/{id}/activate`    | تفعيل فئة          | 🟡 **متوسطة** | **مطلوب** - Admin                 |
| `PATCH /categories/{id}/deactivate`  | تعطيل فئة          | 🟡 **متوسطة** | **مطلوب** - Admin                 |
| `PUT /categories/reorder`            | إعادة ترتيب الفئات | 🟡 **متوسطة** | **مطلوب** - Admin                 |

**النتيجة:** ⚠️ **67% ناقص** - يجب إضافة 8 Actions

---

## 5️⃣ Requests

### المطلوب (20 endpoints):

#### ✅ المطور (7):

1. ✅ `GET /requests` → `getAll()`
2. ✅ `GET /requests/{id}` → `getById()`
3. ✅ `GET /requests/me` → `getMyRequests()`
4. ✅ `POST /requests` → `create()`
5. ✅ `PUT /requests/{id}` → `update()`
6. ✅ `PATCH /requests/{id}/publish` → `publish()`
7. ✅ `PATCH /requests/{id}/close` → `close()`
8. ✅ `PATCH /requests/{id}/cancel` → `cancel()`

#### 🚨 الناقص (12) - **يجب إضافتها فوراً:**

| Endpoint                                           | الوصف              | الأهمية       | السبب                           |
| -------------------------------------------------- | ------------------ | ------------- | ------------------------------- |
| `DELETE /requests/{id}`                            | حذف طلب            | 🔴 **عالية**  | **مطلوب** - حذف الطلب           |
| `PATCH /requests/{id}/extend-deadline`             | تمديد الموعد       | 🔴 **عالية**  | **مطلوب** - تمديد موعد الطلب    |
| `POST /requests/{id}/attachments`                  | رفع مرفق           | 🔴 **عالية**  | **مطلوب** - رفع المرفقات        |
| `DELETE /requests/{id}/attachments/{attachmentId}` | حذف مرفق           | 🟡 **متوسطة** | **مطلوب** - حذف المرفقات        |
| `POST /requests/{id}/select-offer`                 | اختيار عرض فائز    | 🔴 **عالية**  | **مطلوب** - اختيار العرض الفائز |
| `DELETE /requests/{id}/unselect-offer`             | إلغاء اختيار العرض | 🟡 **متوسطة** | **مطلوب** - إلغاء الاختيار      |
| `GET /requests/active`                             | الطلبات النشطة     | 🔴 **عالية**  | **مطلوب** - عرض الطلبات النشطة  |
| `GET /requests/category/{categoryId}`              | طلبات حسب الفئة    | 🔴 **عالية**  | **مطلوب** - البحث حسب الفئة     |
| `GET /requests/{id}/offers`                        | عروض طلب معين      | 🔴 **عالية**  | **مطلوب** - عرض عروض الطلب      |
| `PATCH /requests/{id}/increment-views`             | زيادة المشاهدات    | 🟡 **متوسطة** | **مطلوب** - تتبع المشاهدات      |
| `GET /requests/me/statistics`                      | إحصائيات طلباتي    | 🟡 **متوسطة** | **مطلوب** - Dashboard           |

**النتيجة:** ⚠️ **60% ناقص** - يجب إضافة 12 Actions

---

## 6️⃣ Offers

### المطلوب (15 endpoints):

#### ✅ المطور (7):

1. ✅ `GET /offers` → `getAll()`
2. ✅ `GET /offers/{id}` → `getById()`
3. ✅ `GET /offers/me` → `getMyOffers()`
4. ✅ `GET /offers/request/{requestId}` → `getByRequestId()`
5. ✅ `POST /offers` → `create()`
6. ✅ `PUT /offers/{id}` → `update()`
7. ✅ `PATCH /offers/{id}/withdraw` → `withdraw()`
8. ✅ `PATCH /offers/{id}/accept` → `accept()`
9. ✅ `PATCH /offers/{id}/reject` → `reject()`

#### 🚨 الناقص (6) - **يجب إضافتها فوراً:**

| Endpoint                                         | الوصف          | الأهمية       | السبب                                     |
| ------------------------------------------------ | -------------- | ------------- | ----------------------------------------- |
| `DELETE /offers/{id}`                            | سحب عرض        | 🔴 **عالية**  | **مطلوب** - سحب العرض (مختلف عن withdraw) |
| `POST /offers/{id}/attachments`                  | رفع مرفق       | 🔴 **عالية**  | **مطلوب** - رفع المرفقات                  |
| `DELETE /offers/{id}/attachments/{attachmentId}` | حذف مرفق       | 🟡 **متوسطة** | **مطلوب** - حذف المرفقات                  |
| `PUT /offers/{id}/client-notes`                  | ملاحظات العميل | 🟡 **متوسطة** | **مطلوب** - ملاحظات العميل                |
| `POST /offers/compare`                           | مقارنة العروض  | 🔴 **عالية**  | **مطلوب** - مقارنة العروض                 |
| `GET /offers/{id}/statistics`                    | إحصائيات العرض | 🟡 **متوسطة** | **مطلوب** - Dashboard                     |

**النتيجة:** ⚠️ **40% ناقص** - يجب إضافة 6 Actions

---

## 7️⃣ Projects

### المطلوب (15 endpoints):

#### ✅ المطور (3):

1. ✅ `GET /projects/me` → `getAll()`
2. ✅ `GET /projects/{id}` → `getById()`
3. ✅ `GET /milestones/project/{projectId}` → `getMilestones()`

#### 🚨 الناقص (12) - **يجب إضافتها فوراً:**

| Endpoint                                | الوصف               | الأهمية       | السبب                         |
| --------------------------------------- | ------------------- | ------------- | ----------------------------- |
| `PATCH /projects/{id}/status`           | تحديث حالة المشروع  | 🔴 **عالية**  | **مطلوب** - تحديث الحالة      |
| `PATCH /projects/{id}/start`            | بدء المشروع         | 🔴 **عالية**  | **مطلوب** - بدء المشروع       |
| `PATCH /projects/{id}/complete`         | إتمام المشروع       | 🔴 **عالية**  | **مطلوب** - إتمام المشروع     |
| `PATCH /projects/{id}/cancel`           | إلغاء المشروع       | 🔴 **عالية**  | **مطلوب** - إلغاء المشروع     |
| `PATCH /projects/{id}/pause`            | إيقاف مؤقت          | 🔴 **عالية**  | **مطلوب** - إيقاف المشروع     |
| `PATCH /projects/{id}/resume`           | استئناف المشروع     | 🔴 **عالية**  | **مطلوب** - استئناف المشروع   |
| `PATCH /projects/{id}/progress`         | تحديث نسبة الإنجاز  | 🔴 **عالية**  | **مطلوب** - تحديث التقدم      |
| `POST /projects/{id}/delivery-proof`    | رفع إثبات التسليم   | 🔴 **عالية**  | **مطلوب** - رفع إثبات التسليم |
| `PUT /projects/{id}/delivery-notes`     | ملاحظات التسليم     | 🟡 **متوسطة** | **مطلوب** - ملاحظات التسليم   |
| `PATCH /projects/{id}/approve-delivery` | اعتماد التسليم      | 🔴 **عالية**  | **مطلوب** - اعتماد التسليم    |
| `PATCH /projects/{id}/reject-delivery`  | رفض التسليم         | 🔴 **عالية**  | **مطلوب** - رفض التسليم       |
| `POST /projects/{id}/request-revision`  | طلب تعديل           | 🔴 **عالية**  | **مطلوب** - طلب تعديل         |
| `GET /projects/active`                  | المشاريع النشطة     | 🟡 **متوسطة** | **مطلوب** - البحث             |
| `GET /projects/completed`               | المشاريع المكتملة   | 🟡 **متوسطة** | **مطلوب** - البحث             |
| `GET /projects/status/{status}`         | المشاريع حسب الحالة | 🟡 **متوسطة** | **مطلوب** - البحث             |
| `GET /projects/{id}/timeline`           | الجدول الزمني       | 🟡 **متوسطة** | **مطلوب** - عرض Timeline      |
| `GET /projects/me/statistics`           | إحصائيات مشاريعي    | 🟡 **متوسطة** | **مطلوب** - Dashboard         |

**النتيجة:** ⚠️ **80% ناقص** - يجب إضافة 12 Actions

---

## 8️⃣ Contracts

### المطلوب (12 endpoints):

#### ✅ المطور (3):

1. ✅ `GET /contracts/{id}` → `getById()`
2. ✅ `POST /contracts/{id}/sign` → `sign()`
3. ✅ `GET /contracts/{id}/versions` → `getVersionHistory()`

#### 🚨 الناقص (9) - **يجب إضافتها فوراً:**

| Endpoint                                    | الوصف            | الأهمية       | السبب                       |
| ------------------------------------------- | ---------------- | ------------- | --------------------------- |
| `POST /contracts`                           | إنشاء عقد        | 🔴 **عالية**  | **مطلوب** - إنشاء العقد     |
| `PUT /contracts/{id}`                       | تحديث عقد        | 🔴 **عالية**  | **مطلوب** - تحديث العقد     |
| `DELETE /contracts/{id}`                    | إلغاء عقد        | 🔴 **عالية**  | **مطلوب** - إلغاء العقد     |
| `GET /contracts/project/{projectId}`        | عقد المشروع      | 🔴 **عالية**  | **مطلوب** - عرض عقد المشروع |
| `POST /contracts/{id}/sign/client`          | توقيع العميل     | 🔴 **عالية**  | **مطلوب** - توقيع منفصل     |
| `POST /contracts/{id}/sign/supplier`        | توقيع المورد     | 🔴 **عالية**  | **مطلوب** - توقيع منفصل     |
| `POST /contracts/{id}/versions`             | إنشاء نسخة جديدة | 🟡 **متوسطة** | **مطلوب** - إدارة النسخ     |
| `GET /contracts/{id}/versions/{version}`    | نسخة محددة       | 🟡 **متوسطة** | **مطلوب** - عرض نسخة محددة  |
| `POST /contracts/{id}/clauses`              | إضافة بند مخصص   | 🟡 **متوسطة** | **مطلوب** - إضافة بنود      |
| `DELETE /contracts/{id}/clauses/{clauseId}` | حذف بند          | 🟡 **متوسطة** | **مطلوب** - حذف بنود        |
| `GET /contracts/{id}/download`              | تنزيل PDF        | 🔴 **عالية**  | **مطلوب** - تنزيل العقد     |
| `GET /contracts/{id}/status`                | حالة العقد       | 🟡 **متوسطة** | **مطلوب** - حالة العقد      |

**النتيجة:** ⚠️ **75% ناقص** - يجب إضافة 9 Actions

---

## 9️⃣ Project Milestones

### المطلوب (12 endpoints):

#### ✅ المطور (1):

1. ✅ `GET /milestones/project/{projectId}` → `getMilestones()`

#### 🚨 الناقص (11) - **يجب إضافتها فوراً:**

| Endpoint                                             | الوصف            | الأهمية       | السبب                          |
| ---------------------------------------------------- | ---------------- | ------------- | ------------------------------ |
| `POST /milestones`                                   | إضافة مرحلة      | 🔴 **عالية**  | **مطلوب** - إضافة مرحلة جديدة  |
| `GET /milestones/{id}`                               | عرض مرحلة        | 🔴 **عالية**  | **مطلوب** - عرض تفاصيل المرحلة |
| `PUT /milestones/{id}`                               | تحديث مرحلة      | 🔴 **عالية**  | **مطلوب** - تحديث المرحلة      |
| `DELETE /milestones/{id}`                            | حذف مرحلة        | 🔴 **عالية**  | **مطلوب** - حذف المرحلة        |
| `PATCH /milestones/{id}/complete`                    | إتمام مرحلة      | 🔴 **عالية**  | **مطلوب** - إتمام المرحلة      |
| `PATCH /milestones/{id}/approve`                     | اعتماد مرحلة     | 🔴 **عالية**  | **مطلوب** - اعتماد المرحلة     |
| `PATCH /milestones/{id}/reject`                      | رفض مرحلة        | 🔴 **عالية**  | **مطلوب** - رفض المرحلة        |
| `PATCH /milestones/{id}/release-payment`             | صرف دفعة المرحلة | 🔴 **عالية**  | **مطلوب** - صرف الدفعة         |
| `POST /milestones/{id}/attachments`                  | رفع مرفق         | 🔴 **عالية**  | **مطلوب** - رفع المرفقات       |
| `DELETE /milestones/{id}/attachments/{attachmentId}` | حذف مرفق         | 🟡 **متوسطة** | **مطلوب** - حذف المرفقات       |
| `GET /milestones/project/{projectId}/pending`        | المراحل المعلقة  | 🟡 **متوسطة** | **مطلوب** - البحث              |
| `GET /milestones/project/{projectId}/completed`      | المراحل المكتملة | 🟡 **متوسطة** | **مطلوب** - البحث              |
| `PUT /milestones/project/{projectId}/reorder`        | إعادة الترتيب    | 🟡 **متوسطة** | **مطلوب** - إعادة الترتيب      |

**النتيجة:** 🚨 **92% ناقص** - يجب إضافة 11 Actions

---

## 🔟 Payments

### المطلوب (15 endpoints):

#### ✅ المطور (5):

1. ✅ `GET /payments/{id}` → `getById()`
2. ✅ `GET /payments/transactions` → `getTransactions()`
3. ✅ `GET /payments/wallet` → `getWallet()`
4. ✅ `POST /payments/add-funds` → `addFunds()`
5. ✅ `POST /payments/withdraw` → `withdrawFunds()`

#### 🚨 الناقص (10) - **يجب إضافتها فوراً:**

| Endpoint                             | الوصف           | الأهمية       | السبب                           |
| ------------------------------------ | --------------- | ------------- | ------------------------------- |
| `POST /payments/initiate`            | بدء عملية دفع   | 🔴 **عالية**  | **مطلوب** - بدء عملية الدفع     |
| `POST /payments/{id}/process`        | معالجة الدفع    | 🔴 **عالية**  | **مطلوب** - معالجة الدفع        |
| `POST /payments/{id}/confirm`        | تأكيد الدفع     | 🔴 **عالية**  | **مطلوب** - تأكيد الدفع         |
| `DELETE /payments/{id}/cancel`       | إلغاء الدفع     | 🔴 **عالية**  | **مطلوب** - إلغاء الدفع         |
| `POST /payments/{id}/refund`         | استرجاع كامل    | 🔴 **عالية**  | **مطلوب** - استرجاع المبلغ      |
| `POST /payments/{id}/refund/partial` | استرجاع جزئي    | 🔴 **عالية**  | **مطلوب** - استرجاع جزئي        |
| `POST /payments/{id}/release`        | تحويل للمورد    | 🔴 **عالية**  | **مطلوب** - تحويل المبلغ للمورد |
| `GET /payments/me`                   | دفعاتي          | 🔴 **عالية**  | **مطلوب** - عرض دفعاتي          |
| `GET /payments/project/{projectId}`  | دفعات مشروع     | 🔴 **عالية**  | **مطلوب** - عرض دفعات المشروع   |
| `GET /payments/history`              | سجل الدفعات     | 🟡 **متوسطة** | **مطلوب** - سجل الدفعات         |
| `GET /payments/pending`              | الدفعات المعلقة | 🟡 **متوسطة** | **مطلوب** - البحث               |
| `GET /payments/failed`               | الدفعات الفاشلة | 🟡 **متوسطة** | **مطلوب** - البحث               |
| `GET /payments/statistics`           | إحصائيات الدفع  | 🟡 **متوسطة** | **مطلوب** - Dashboard           |
| `POST /payments/calculate-fee`       | حساب العمولة    | 🟡 **متوسطة** | **مطلوب** - حساب العمولة        |

**النتيجة:** ⚠️ **67% ناقص** - يجب إضافة 10 Actions

---

## 1️⃣1️⃣ Reviews

### المطلوب (15 endpoints):

#### ✅ المطور (4):

1. ✅ `GET /reviews` → `getAll()`
2. ✅ `GET /reviews/{id}` → `getById()`
3. ✅ `POST /reviews` → `create()`
4. ✅ `PUT /reviews/{id}` → `update()`

#### 🚨 الناقص (11) - **يجب إضافتها فوراً:**

| Endpoint                                        | الوصف            | الأهمية       | السبب                            |
| ----------------------------------------------- | ---------------- | ------------- | -------------------------------- |
| `DELETE /reviews/{id}`                          | حذف تقييم        | 🔴 **عالية**  | **مطلوب** - حذف التقييم          |
| `POST /reviews/{id}/respond`                    | الرد على تقييم   | 🔴 **عالية**  | **مطلوب** - الرد على التقييم     |
| `PATCH /reviews/{id}/helpful`                   | تقييم مفيد       | 🟡 **متوسطة** | **مطلوب** - تفاعل المستخدمين     |
| `PATCH /reviews/{id}/not-helpful`               | تقييم غير مفيد   | 🟡 **متوسطة** | **مطلوب** - تفاعل المستخدمين     |
| `POST /reviews/{id}/report`                     | الإبلاغ عن تقييم | 🟡 **متوسطة** | **مطلوب** - الإبلاغ              |
| `GET /reviews/project/{projectId}`              | تقييمات مشروع    | 🔴 **عالية**  | **مطلوب** - عرض تقييمات المشروع  |
| `GET /reviews/user/{userId}`                    | تقييمات مستخدم   | 🔴 **عالية**  | **مطلوب** - عرض تقييمات المستخدم |
| `GET /reviews/supplier/{supplierId}`            | تقييمات مورد     | 🔴 **عالية**  | **مطلوب** - عرض تقييمات المورد   |
| `GET /reviews/supplier/{supplierId}/average`    | متوسط التقييم    | 🔴 **عالية**  | **مطلوب** - متوسط التقييم        |
| `GET /reviews/supplier/{supplierId}/statistics` | إحصائيات         | 🟡 **متوسطة** | **مطلوب** - Dashboard            |
| `GET /reviews/top-rated`                        | التقييمات الأعلى | 🟡 **متوسطة** | **مطلوب** - الصفحة الرئيسية      |

**النتيجة:** ⚠️ **73% ناقص** - يجب إضافة 11 Actions

---

## 1️⃣2️⃣ Messages & Conversations

### المطلوب (12 endpoints):

#### ✅ المطور (3):

1. ✅ `GET /messages/conversations` → `getConversations()`
2. ✅ `GET /messages/conversations/{id}` → `getConversation()`
3. ✅ `POST /messages/conversations/{id}/messages` → `sendMessage()`

#### 🚨 الناقص (9) - **يجب إضافتها فوراً:**

| Endpoint                                           | الوصف                    | الأهمية       | السبب                             |
| -------------------------------------------------- | ------------------------ | ------------- | --------------------------------- |
| `POST /conversations`                              | بدء محادثة               | 🔴 **عالية**  | **مطلوب** - بدء محادثة جديدة      |
| `PATCH /conversations/{id}/close`                  | إغلاق محادثة             | 🟡 **متوسطة** | **مطلوب** - إغلاق المحادثة        |
| `PATCH /conversations/{id}/archive`                | أرشفة محادثة             | 🟡 **متوسطة** | **مطلوب** - أرشفة                 |
| `PATCH /conversations/{id}/unarchive`              | إلغاء الأرشفة            | 🟡 **متوسطة** | **مطلوب** - إلغاء الأرشفة         |
| `PATCH /conversations/{id}/mute`                   | كتم الإشعارات            | 🟡 **متوسطة** | **مطلوب** - كتم الإشعارات         |
| `PATCH /conversations/{id}/unmute`                 | إلغاء الكتم              | 🟡 **متوسطة** | **مطلوب** - إلغاء الكتم           |
| `GET /conversations/{id}/messages`                 | رسائل المحادثة           | 🔴 **عالية**  | **مطلوب** - عرض الرسائل           |
| `PUT /messages/{id}`                               | تعديل رسالة              | 🟡 **متوسطة** | **مطلوب** - تعديل الرسالة         |
| `DELETE /messages/{id}`                            | حذف رسالة                | 🟡 **متوسطة** | **مطلوب** - حذف الرسالة           |
| `PATCH /messages/{id}/read`                        | تعليم كمقروء             | 🔴 **عالية**  | **مطلوب** - تعليم الرسالة كمقروءة |
| `PATCH /conversations/{id}/messages/read-all`      | تعليم الكل كمقروء        | 🔴 **عالية**  | **مطلوب** - تعليم الكل            |
| `POST /messages/{id}/attachments`                  | رفع مرفق                 | 🔴 **عالية**  | **مطلوب** - رفع المرفقات          |
| `DELETE /messages/{id}/attachments/{attachmentId}` | حذف مرفق                 | 🟡 **متوسطة** | **مطلوب** - حذف المرفقات          |
| `POST /conversations/{id}/system-message`          | إرسال رسالة نظام         | 🟡 **متوسطة** | **مطلوب** - رسائل النظام          |
| `GET /messages/unread/count`                       | عدد الرسائل غير المقروءة | 🔴 **عالية**  | **مطلوب** - عداد الرسائل          |
| `GET /messages/search`                             | البحث في الرسائل         | 🟡 **متوسطة** | **مطلوب** - البحث                 |

**النتيجة:** ⚠️ **75% ناقص** - يجب إضافة 9 Actions

---

## 1️⃣3️⃣ Notifications

### المطلوب (10 endpoints):

#### ✅ المطور (4):

1. ✅ `GET /notifications` → `getAll()`
2. ✅ `PATCH /notifications/{id}/read` → `markAsRead()`
3. ✅ `PATCH /notifications/read-all` → `markAllAsRead()`
4. ✅ `GET /notifications/unread-count` → `getUnreadCount()`

#### 🚨 الناقص (6) - **يجب إضافتها فوراً:**

| Endpoint                          | الوصف                  | الأهمية       | السبب                          |
| --------------------------------- | ---------------------- | ------------- | ------------------------------ |
| `GET /notifications/{id}`         | عرض إشعار              | 🟡 **متوسطة** | **مطلوب** - عرض تفاصيل الإشعار |
| `DELETE /notifications/{id}`      | حذف إشعار              | 🟡 **متوسطة** | **مطلوب** - حذف الإشعار        |
| `DELETE /notifications/clear-all` | حذف جميع الإشعارات     | 🟡 **متوسطة** | **مطلوب** - حذف الكل           |
| `GET /notifications/unread`       | الإشعارات غير المقروءة | 🔴 **عالية**  | **مطلوب** - عرض غير المقروءة   |
| `GET /notifications/type/{type}`  | الإشعارات حسب النوع    | 🟡 **متوسطة** | **مطلوب** - البحث              |

**النتيجة:** ⚠️ **60% ناقص** - يجب إضافة 6 Actions

---

## 1️⃣4️⃣ Disputes

### المطلوب (12 endpoints):

#### ✅ المطور (3):

1. ✅ `GET /disputes` → `getAll()`
2. ✅ `GET /disputes/{id}` → `getById()`
3. ✅ `POST /disputes` → `create()`
4. ✅ `POST /disputes/{id}/evidence` → `addEvidence()`

#### 🚨 الناقص (8) - **يجب إضافتها فوراً:**

| Endpoint                       | الوصف        | الأهمية      | السبب                          |
| ------------------------------ | ------------ | ------------ | ------------------------------ |
| `GET /disputes/me`             | نزاعاتي      | 🔴 **عالية** | **مطلوب** - عرض نزاعاتي        |
| `PUT /disputes/{id}`           | تحديث نزاع   | 🔴 **عالية** | **مطلوب** - تحديث النزاع       |
| `PATCH /disputes/{id}/close`   | إغلاق نزاع   | 🔴 **عالية** | **مطلوب** - إغلاق النزاع       |
| `POST /disputes/{id}/messages` | إضافة رسالة  | 🔴 **عالية** | **مطلوب** - إضافة رسالة للنزاع |
| `GET /disputes/{id}/messages`  | رسائل النزاع | 🔴 **عالية** | **مطلوب** - عرض رسائل النزاع   |

**النتيجة:** ⚠️ **67% ناقص** - يجب إضافة 8 Actions

---

## 📊 ملخص النواقص الخطيرة

### 🔴 Actions المفقودة بالكامل (يجب إضافتها فوراً):

#### Authentication (13 missing):

-   change-password, verify-phone, resend-verification, logout-all
-   2FA (enable, disable, verify, backup-codes)

#### Users (12 missing):

-   delete account, deactivate, reactivate
-   avatar upload/delete, address, notification-preferences
-   statistics, wallet balance, public profile

#### Suppliers (12 missing):

-   create profile, delete profile
-   certifications (add, delete), working-hours
-   verification request/cancel, earnings, performance
-   reviews, top-rated, category search

#### Categories (8 missing):

-   tree, active, subcategories, parent
-   delete, activate, deactivate, reorder

#### Requests (12 missing):

-   delete, extend-deadline
-   attachments (upload, delete)
-   select-offer, unselect-offer
-   active, category search, offers, increment-views, statistics

#### Offers (6 missing):

-   delete, attachments (upload, delete)
-   client-notes, compare, statistics

#### Projects (12 missing):

-   status, start, complete, cancel, pause, resume
-   progress, delivery-proof, delivery-notes
-   approve-delivery, reject-delivery, request-revision
-   active, completed, status filter, timeline, statistics

#### Contracts (9 missing):

-   create, update, delete
-   project contract, sign (client/supplier separate)
-   versions (create, get specific)
-   clauses (add, delete), download PDF, status

#### Milestones (11 missing):

-   create, get, update, delete
-   complete, approve, reject
-   release-payment, attachments (upload, delete)
-   pending, completed, reorder

#### Payments (10 missing):

-   initiate, process, confirm, cancel
-   refund (full, partial), release
-   me, project payments, history
-   pending, failed, statistics, calculate-fee

#### Reviews (11 missing):

-   delete, respond
-   helpful, not-helpful, report
-   project reviews, user reviews, supplier reviews
-   average, statistics, top-rated

#### Messages (9 missing):

-   create conversation, close, archive, unarchive
-   mute, unmute, get messages
-   update message, delete message
-   read, read-all, attachments, system-message
-   unread count, search

#### Notifications (6 missing):

-   get by id, delete, clear-all
-   unread, type filter

#### Disputes (8 missing):

-   me, update, close
-   messages (add, get)

---

## 🚨 تحذيرات خطيرة (Critical Warnings)

### ⚠️ **تحذير 1: Milestones - ناقص 92%**

**الخطورة:** 🔴 **عالية جداً**

**المشكلة:**

-   فقط `getMilestones()` موجود
-   **11 Action ناقص** من أصل 12
-   **لا يمكن إدارة المراحل** بدون هذه Actions

**النتيجة:** ❌ **لا يمكن إدارة المشاريع** - المشاريع تحتاج مراحل!

**يجب إضافة فوراً:**

-   `createMilestone()` - إضافة مرحلة
-   `updateMilestone()` - تحديث مرحلة
-   `deleteMilestone()` - حذف مرحلة
-   `completeMilestone()` - إتمام مرحلة
-   `approveMilestone()` - اعتماد مرحلة
-   `rejectMilestone()` - رفض مرحلة
-   `releaseMilestonePayment()` - صرف دفعة المرحلة
-   `uploadMilestoneAttachment()` - رفع مرفق
-   `deleteMilestoneAttachment()` - حذف مرفق

---

### ⚠️ **تحذير 2: Projects - ناقص 80%**

**الخطورة:** 🔴 **عالية جداً**

**المشكلة:**

-   فقط `getAll()`, `getById()`, `getMilestones()` موجود
-   **12 Action ناقص** من أصل 15
-   **لا يمكن إدارة المشاريع** بدون هذه Actions

**النتيجة:** ❌ **لا يمكن إدارة المشاريع** - المشاريع هي قلب النظام!

**يجب إضافة فوراً:**

-   `updateProjectStatus()` - تحديث الحالة
-   `startProject()` - بدء المشروع
-   `completeProject()` - إتمام المشروع
-   `cancelProject()` - إلغاء المشروع
-   `pauseProject()` - إيقاف المشروع
-   `resumeProject()` - استئناف المشروع
-   `updateProgress()` - تحديث التقدم
-   `uploadDeliveryProof()` - رفع إثبات التسليم
-   `approveDelivery()` - اعتماد التسليم
-   `rejectDelivery()` - رفض التسليم
-   `requestRevision()` - طلب تعديل

---

### ⚠️ **تحذير 3: Payments - ناقص 67%**

**الخطورة:** 🔴 **عالية جداً**

**المشكلة:**

-   فقط `getById()`, `getTransactions()`, `getWallet()`, `addFunds()`, `withdrawFunds()` موجود
-   **10 Actions ناقص** من أصل 15
-   **لا يمكن معالجة الدفعات** بدون هذه Actions

**النتيجة:** ❌ **لا يمكن معالجة الدفعات** - الدفعات هي أساس النظام!

**يجب إضافة فوراً:**

-   `initiatePayment()` - بدء عملية دفع
-   `processPayment()` - معالجة الدفع
-   `confirmPayment()` - تأكيد الدفع
-   `cancelPayment()` - إلغاء الدفع
-   `refundPayment()` - استرجاع كامل
-   `refundPartial()` - استرجاع جزئي
-   `releasePayment()` - تحويل للمورد
-   `getMyPayments()` - دفعاتي
-   `getProjectPayments()` - دفعات المشروع
-   `calculateFee()` - حساب العمولة

---

### ⚠️ **تحذير 4: Contracts - ناقص 75%**

**الخطورة:** 🔴 **عالية جداً**

**المشكلة:**

-   فقط `getById()`, `sign()`, `getVersionHistory()` موجود
-   **9 Actions ناقص** من أصل 12
-   **لا يمكن إدارة العقود** بدون هذه Actions

**النتيجة:** ❌ **لا يمكن إدارة العقود** - العقود مطلوبة للمشاريع!

**يجب إضافة فوراً:**

-   `createContract()` - إنشاء عقد
-   `updateContract()` - تحديث عقد
-   `deleteContract()` - إلغاء عقد
-   `getProjectContract()` - عقد المشروع
-   `signAsClient()` - توقيع العميل
-   `signAsSupplier()` - توقيع المورد
-   `createVersion()` - إنشاء نسخة
-   `getVersion()` - نسخة محددة
-   `addClause()` - إضافة بند
-   `deleteClause()` - حذف بند
-   `downloadContract()` - تنزيل PDF
-   `getContractStatus()` - حالة العقد

---

### ⚠️ **تحذير 5: Requests - ناقص 60%**

**الخطورة:** 🔴 **عالية**

**المشكلة:**

-   **12 Actions ناقص** من أصل 20
-   **لا يمكن إدارة الطلبات بشكل كامل** بدون هذه Actions

**يجب إضافة فوراً:**

-   `deleteRequest()` - حذف طلب
-   `extendDeadline()` - تمديد الموعد
-   `uploadAttachment()` - رفع مرفق
-   `deleteAttachment()` - حذف مرفق
-   `selectOffer()` - اختيار عرض فائز
-   `unselectOffer()` - إلغاء اختيار العرض
-   `getActiveRequests()` - الطلبات النشطة
-   `getRequestsByCategory()` - طلبات حسب الفئة
-   `getRequestOffers()` - عروض الطلب
-   `incrementViews()` - زيادة المشاهدات
-   `getMyStatistics()` - إحصائياتي

---

## 📋 قائمة Actions المطلوبة حسب الأولوية

### 🔴 أولوية عالية جداً (يجب إضافتها فوراً):

#### Milestones (11):

1. `createMilestone()`
2. `updateMilestone()`
3. `deleteMilestone()`
4. `completeMilestone()`
5. `approveMilestone()`
6. `rejectMilestone()`
7. `releaseMilestonePayment()`
8. `uploadMilestoneAttachment()`
9. `deleteMilestoneAttachment()`
10. `getPendingMilestones()`
11. `getCompletedMilestones()`

#### Projects (12):

1. `updateProjectStatus()`
2. `startProject()`
3. `completeProject()`
4. `cancelProject()`
5. `pauseProject()`
6. `resumeProject()`
7. `updateProgress()`
8. `uploadDeliveryProof()`
9. `approveDelivery()`
10. `rejectDelivery()`
11. `requestRevision()`
12. `getProjectStatistics()`

#### Payments (10):

1. `initiatePayment()`
2. `processPayment()`
3. `confirmPayment()`
4. `cancelPayment()`
5. `refundPayment()`
6. `refundPartial()`
7. `releasePayment()`
8. `getMyPayments()`
9. `getProjectPayments()`
10. `calculateFee()`

#### Contracts (9):

1. `createContract()`
2. `updateContract()`
3. `deleteContract()`
4. `getProjectContract()`
5. `signAsClient()`
6. `signAsSupplier()`
7. `downloadContract()`
8. `getContractStatus()`
9. `addClause()`

---

### 🟡 أولوية متوسطة (يجب إضافتها قريباً):

#### Authentication (5):

-   `changePassword()`
-   `verifyPhone()`
-   `resendVerification()`
-   `logoutAll()`
-   `enable2FA()`, `disable2FA()`, `verify2FA()`

#### Users (8):

-   `deleteAccount()`
-   `deactivateAccount()`
-   `reactivateAccount()`
-   `uploadAvatar()`
-   `deleteAvatar()`
-   `updateAddress()`
-   `updateNotificationPreferences()`
-   `getStatistics()`

#### Suppliers (8):

-   `createProfile()`
-   `deleteProfile()`
-   `addCertification()`
-   `deleteCertification()`
-   `updateWorkingHours()`
-   `requestVerification()`
-   `getEarnings()`
-   `getPerformance()`

#### Requests (8):

-   `deleteRequest()`
-   `extendDeadline()`
-   `uploadAttachment()`
-   `deleteAttachment()`
-   `selectOffer()`
-   `unselectOffer()`
-   `getActiveRequests()`
-   `getRequestsByCategory()`

---

## 🎯 التوصيات النهائية

### ✅ ما يجب فعله فوراً:

1. **إضافة Milestones Actions** (11 actions) - 🔴 **أولوية عالية جداً**
2. **إضافة Projects Actions** (12 actions) - 🔴 **أولوية عالية جداً**
3. **إضافة Payments Actions** (10 actions) - 🔴 **أولوية عالية جداً**
4. **إضافة Contracts Actions** (9 actions) - 🔴 **أولوية عالية جداً**

**المجموع:** **42 Action** يجب إضافتها فوراً

### ⚠️ ما يجب فعله قريباً:

5. **إضافة Requests Actions** (8 actions)
6. **إضافة Authentication Actions** (5 actions)
7. **إضافة Users Actions** (8 actions)
8. **إضافة Suppliers Actions** (8 actions)

**المجموع:** **29 Action** يجب إضافتها قريباً

---

## 📊 الإحصائيات النهائية

### النتيجة الإجمالية:

-   **المطلوب:** 200+ Actions
-   **المطور:** 61 Actions
-   **الناقص:** 139 Actions
-   **النسبة:** 30.5% مطور

### حسب الأولوية:

-   🔴 **أولوية عالية جداً:** 42 Actions
-   🟡 **أولوية متوسطة:** 29 Actions
-   🟢 **أولوية منخفضة:** 68 Actions

---

## 🚨 الخلاصة النهائية

**الحالة:** ⚠️ **ناقص خطير** - **69.5% من Actions غير موجودة**

**النتيجة:** Frontend **لا يمكنه العمل بشكل كامل** بدون هذه Actions.

**يجب إضافة:** **139 Action** على الأقل للوصول إلى 100%.

**الوقت المقدر:** 3-4 أسابيع عمل مكثف لإضافة جميع Actions المطلوبة.

---

**تم إعداد التقرير بواسطة:** AI Project Manager  
**التاريخ:** 2025-01-27  
**حالة التقرير:** 🚨 **تحذير خطير**
