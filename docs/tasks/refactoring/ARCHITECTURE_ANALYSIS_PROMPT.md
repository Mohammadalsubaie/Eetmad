برومبت تحليل البنية المعمارية للمكونات في Next.js
markdown

أنت خبير معماري في React و Next.js متخصص في تحليل جودة الكود وإعادة الهيكلة (Refactoring).

# المهمة الرئيسية:

قم بتحليل شامل ودقيق لبنية المكونات في المشروع المرفق، مع التركيز على اكتشاف **الانتهاكات المعمارية الخطيرة** حيث تم بناء مكونات UI أساسية (primitives) فوق مكونات تطبيقية معقدة (business components)، وهو عكس المبادئ الصحيحة للتصميم.

# معايير التحليل الأساسية:

## 1. تصنيف المكونات حسب المستوى المعماري:

### المستوى 1 - UI Primitives (المكونات الأساسية البدائية):

يجب أن تكون مستقلة تماماً ولا تعتمد على أي مكونات تطبيقية:

-   Button, Input, Select, Checkbox, Radio, Switch
-   Card, Modal, Dialog, Drawer, Popover, Tooltip
-   Badge, Avatar, Chip, Tag
-   Alert, Toast, Notification
-   Spinner, Skeleton, Progress
-   Divider, Separator
-   Table (الجدول الأساسي فقط)
-   Form (النموذج الأساسي فقط)
-   Typography (Text, Heading, Paragraph)
-   Layout Primitives (Box, Stack, Grid, Flex)

### المستوى 2 - Composite Components (المكونات المركبة):

يمكن أن تستخدم UI Primitives، لكن لا تحتوي على منطق تطبيقي:

-   FormField, FormGroup
-   DataTable (مع ميزات متقدمة)
-   Breadcrumb
-   Stepper, Wizard
-   Menu, DropdownMenu
-   Tabs, TabPanel
-   Calendar, DatePicker

### المستوى 3 - Feature Components (مكونات الميزات):

تستخدم Primitives و Composites، وتحتوي على منطق تطبيقي محدد:

-   LoginForm, SignupForm, ContactForm
-   UserProfile, UserSettings
-   ProductList, ProductGrid
-   SearchBar, FilterPanel
-   CommentSection, ReviewList

### المستوى 4 - Business/Domain Components (المكونات التطبيقية):

تجمع بين عدة Feature Components وتحتوي على منطق الأعمال:

-   Dashboard, DashboardStats
-   UserCard, SupplierCard, ProductCard
-   OrderList, InvoiceList
-   StatSection, AnalyticsPanel
-   Sidebar, Header, Footer, Navigation

### المستوى 5 - Page Components (مكونات الصفحات):

تجمع كل المستويات السابقة لتشكيل صفحة كاملة:

-   HomePage, DashboardPage, ProfilePage
-   SettingsPage, CheckoutPage

## 2. أنماط الانتهاكات المعمارية المطلوب كشفها:

### 🔴 انتهاكات حرجة (Critical Violations):

-   **مكون UI أساسي يستورد مكون تطبيقي**:
    مثال: `Button` يستورد `UserCard` أو `StatSection`
-   **مكون UI أساسي يحتوي على منطق أعمال**:
    مثال: `Input` يحتوي على منطق التحقق من صحة البيانات الخاصة بالمستخدمين
-   **مكون UI أساسي مرتبط بـ API أو State Management**:
    مثال: `Card` يقوم باستدعاء API مباشرة أو يستخدم Redux/Context خاص بالتطبيق

### 🟠 انتهاكات متوسطة (Medium Violations):

-   **Composite Component يستورد Business Component**:
    مثال: `DataTable` يستورد `SupplierCard`
-   **Circular Dependencies بين المكونات**:
    مثال: `ComponentA` يستورد `ComponentB` والعكس
-   **مكون UI يحتوي على Hardcoded Business Logic**:
    مثال: `Select` يحتوي على قائمة ثابتة للمدن أو الفئات

### 🟡 انتهاكات بسيطة (Minor Violations):

-   **تكرار كود UI بدلاً من استخدام Primitives**:
    عدة مكونات تعيد تنفيذ نفس الـ Button بدلاً من استخدام المكون الأساسي
-   **Props Drilling عميق جداً**:
    تمرير Props عبر أكثر من 3-4 مستويات

## 3. التحليلات المطلوبة بالتفصيل:

### أ) Dependency Graph Analysis:

-   ارسم خريطة اعتماديات (dependency tree) لكل مكون
-   حدد المكونات التي تستورد مكونات من مستوى أعلى (Wrong Direction)
-   احسب عمق الاعتماديات (Dependency Depth) لكل مكون

### ب) Component Coupling Analysis:

-   قس درجة الترابط (Coupling) بين المكونات
-   حدد المكونات ذات الترابط العالي (Highly Coupled)
-   اقترح فرص لتقليل الترابط

### ج) Reusability Score:

-   احسب درجة إعادة الاستخدام لكل مكون (0-100)
-   حدد المكونات التي يجب أن تكون قابلة لإعادة الاستخدام لكنها ليست كذلك
-   حدد المكونات المستخدمة في مكان واحد فقط لكنها معقدة جداً

### د) Abstraction Level Analysis:

-   تحقق من مستوى التجريد (Abstraction) لكل مكون
-   حدد المكونات التي تخلط بين مستويات التجريد المختلفة
-   اقترح فصل المستويات

## 4. الإحصائيات المطلوبة:

قدم الأرقام التالية:

-   عدد المكونات في كل مستوى معماري
-   عدد الانتهاكات من كل نوع (Critical, Medium, Minor)
-   نسبة المكونات القابلة لإعادة الاستخدام الفعلية
-   متوسط عمق الاعتماديات
-   عدد الـ Circular Dependencies
-   نسبة التغطية بالـ UI Primitives (Component Coverage)
-   Component Duplication Rate (نسبة تكرار الكود)

## 5. التقييم والتوصيات:

### نظام التقييم (Scoring System):

-   احسب نقاط لكل فئة (0-100):
    -   Architecture Score (درجة البنية المعمارية)
    -   Reusability Score (درجة إعادة الاستخدام)
    -   Maintainability Score (درجة قابلية الصيانة)
    -   Overall Health Score (الدرجة الإجمالية)

### التصنيف النهائي:

-   90-100: **ممتاز** - بنية معمارية قوية جداً
-   75-89: **جيد جداً** - بنية جيدة مع تحسينات بسيطة مطلوبة
-   60-74: **مقبول** - يحتاج إلى Refactoring متوسط
-   40-59: **ضعيف** - يحتاج إلى Refactoring كبير
-   0-39: **سيء جداً** - يحتاج إلى إعادة هيكلة كاملة

### الأولويات:

رتب الانتهاكات حسب:

1. التأثير على المشروع (Impact)
2. صعوبة الإصلاح (Effort)
3. المخاطر (Risk)

## 6. خطة الإصلاح المقترحة:

### Phase 1 - Quick Wins (الأسبوع 1-2):

-   قائمة بالإصلاحات السريعة والسهلة
-   الانتهاكات الحرجة التي يمكن إصلاحها بسرعة

### Phase 2 - Medium Refactoring (الأسبوع 3-6):

-   إعادة هيكلة المكونات المتوسطة
-   فصل الاعتماديات الدائرية

### Phase 3 - Major Restructuring (الأسبوع 7-12):

-   إعادة بناء البنية المعمارية الأساسية
-   إنشاء Design System كامل

## 7. أمثلة على الكود:

قدم أمثلة محددة من الكود على:

-   انتهاكات معمارية مع الشرح
-   الكود الصحيح المقترح
-   Before/After للمكونات المهمة

## 8. Best Practices المقترحة:

اقترح:

-   هيكل مجلدات محسّن
-   Naming Conventions
-   Component API Design Guidelines
-   Documentation Standards
-   Testing Strategy

# صيغة التقرير المطلوبة:

📊 تقرير تحليل البنية المعمارية للمكونات
📈 الإحصائيات العامة
[الأرقام والنسب]

🏗️ تصنيف المكونات
[جدول يوضح كل مكون ومستواه المعماري]

🔴 الانتهاكات المعمارية الحرجة
[قائمة تفصيلية مع أمثلة الكود]

🟠 الانتهاكات المتوسطة
[قائمة تفصيلية]

🟡 الانتهاكات البسيطة
[قائمة تفصيلية]

📊 Dependency Graph
[رسم بياني أو وصف نصي للاعتماديات]

💯 نظام التقييم
Architecture Score: X/100
Reusability Score: X/100
Maintainability Score: X/100
Overall Health Score: X/100 - [التصنيف]
🎯 التوصيات ذات الأولوية
[مرتبة حسب الأهمية]

📋 خطة الإصلاح المقترحة
[مقسمة على مراحل زمنية]

💡 أمثلة من الكود
[Before/After للتحسينات المقترحة]

📚 Best Practices
[إرشادات للمستقبل]

✅ الخلاصة والتقييم النهائي
[هل المشروع مقبول؟ مرفوض؟ يحتاج تحسين؟]

# ملاحظات مهمة:

-   كن صريحاً وواضحاً في تقييمك
-   استخدم أمثلة محددة من الكود الفعلي
-   قدم أرقام دقيقة وليس تقديرات عامة
-   رتب التوصيات حسب الأولوية الفعلية
-   كن عملياً في خطة الإصلاح (لا تقترح إعادة كتابة كل شيء إلا إذا كان ضرورياً)

الآن، ابدأ التحليل الشامل للمشروع.
