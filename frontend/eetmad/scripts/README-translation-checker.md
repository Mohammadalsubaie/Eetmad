# Translation Checker Script

## الوصف

سكريبت بسيط وواضح لفحص الترجمات في المشروع. يقوم السكريبت بـ:

1. فحص جميع الملفات في `src/app/[locale]` و `src/components`
2. استخراج جميع استخدامات `t()` و `tPages()` وغيرها
3. التحقق من وجود جميع المفاتيح المستخدمة في ملفات الترجمة (`ar.json` و `en.json`)
4. إنشاء تقرير مفصل بالمفاتيح المفقودة

## الاستخدام

```bash
# من مجلد frontend/eetmad
node scripts/check-translations.js
```

## الخيارات

- `--show-unused`: عرض المفاتيح الموجودة في ملفات الترجمة ولكن غير مستخدمة في الكود

```bash
node scripts/check-translations.js --show-unused
```

## المخرجات

1. **تقرير في الـ Console**: يعرض ملخص سريع للمفاتيح المفقودة
2. **ملف JSON**: `translation-check-report.txt` يحتوي على:
   - إحصائيات شاملة
   - قائمة المفاتيح المفقودة في `ar.json`
   - قائمة المفاتيح المفقودة في `en.json`
   - قائمة المفاتيح غير المستخدمة (إذا تم استخدام `--show-unused`)

## مثال على التقرير

```
📊 ملخص الإحصائيات:
   - إجمالي المفاتيح المستخدمة: 1519
   - المفاتيح المفقودة في ar.json: 474
   - المفاتيح المفقودة في en.json: 277
   - المفاتيح غير المستخدمة: 506 (ar), 584 (en)
```

## ملاحظات

- السكريبت يفحص فقط المفاتيح الثابتة (static keys)
- المفاتيح الديناميكية (مثل `t(\`namespace.${variable}\`)`) قد لا يتم اكتشافها بشكل صحيح
- السكريبت يتحقق من وجود المفاتيح في البنية المتداخلة لملفات JSON

## كيفية إصلاح المشاكل

1. **مفاتيح مفقودة**: أضف المفاتيح المفقودة إلى ملفات الترجمة (`ar.json` و `en.json`)
2. **مفاتيح غير مستخدمة**: يمكنك حذفها أو الاحتفاظ بها للاستخدام المستقبلي

## مثال على إضافة مفتاح مفقود

إذا كان المفتاح المفقود هو `pages.analytics.title`:

```json
{
  "pages": {
    "analytics": {
      "title": "التحليلات"
    }
  }
}
```
