# سكريبت فحص قواعد البناء (Design Rules Validator)

## 📖 نظرة عامة

هذا السكريبت يفحص ملفات المشروع للتأكد من اتباع جميع قواعد البناء المذكورة في:
`frontend/eetmad/docs/design/component-building-guidelines.md`

## 🎯 القواعد المفحوصة

### ✅ Rule 1: Theme System Usage

- اكتشاف الألوان الـ hardcoded (hex colors مثل `#FAF8F1`)
- اكتشاف قيم `rgba/rgb` المباشرة
- اكتشاف Tailwind arbitrary values للألوان (مثل `bg-[#FAF8F1]`)
- التحقق من استخدام `cssVars` بدلاً من `colors`

### ✅ Rule 2: Internationalization (i18n)

- اكتشاف النصوص الـ hardcoded (عربي أو إنجليزي)
- التحقق من استخدام `useTranslations` من `next-intl`
- التحقق من وجود imports مناسبة

### ✅ Rule 3: Component Structure

- التحقق من تطابق اسم Component مع اسم الملف
- التحقق من وجود `'use client'` عند استخدام client hooks
- التحقق من اتباع PascalCase

### ✅ Rule 4: Styling Best Practices

- اكتشاف استخدام Tailwind color classes بدلاً من cssVars
- التحقق من استخدام inline styles مع cssVars

### ✅ Rule 6: Animations and Interactions

- التحقق من استخدام `framer-motion` للـ animations

### ✅ Rule 7: RTL Support

- اكتشاف استخدام directional classes (left/right)
- التوصية باستخدام logical properties (start/end)

## 🚀 الاستخدام

### 1️⃣ فحص ملف واحد

```bash
npm run validate:design src/components/features/home/HeroSection.tsx
```

### 2️⃣ فحص مجلد كامل

```bash
npm run validate:design src/components/features/home/
```

### 3️⃣ فحص عدة ملفات/مجلدات

```bash
npm run validate:design src/app/[locale]/page.tsx src/components/features/home/
```

### 4️⃣ تصدير النتائج إلى JSON

```bash
npm run validate:design src/components/ --json report.json
```

### 5️⃣ عرض المساعدة

```bash
npm run validate:design --help
```

## 📊 أمثلة على النتائج

### مثال على ناتج الفحص:

```
🔍 بدء فحص قواعد البناء...

📁 عدد الملفات المراد فحصها: 15

⏳ جاري الفحص... 15/15

✅ انتهى الفحص!

═══════════════════════════════════════════════════════════════════════
📊 ملخص النتائج
═══════════════════════════════════════════════════════════════════════
📁 إجمالي الملفات: 15
❌ إجمالي الأخطاء: 8
⚠️  إجمالي التحذيرات: 12
═══════════════════════════════════════════════════════════════════════

📝 الملفات التي بها مشاكل: 5/15

────────────────────────────────────────────────────────────────────────
📄 [1/5] src/components/features/home/HeroSection.tsx
   الأخطاء: 3 | التحذيرات: 2 | إجمالي: 5
────────────────────────────────────────────────────────────────────────

❌ الأخطاء:

  1. السطر 45:12
     القاعدة: Rule 1: Theme System Usage
     المشكلة: استخدام لون hex مباشر: #FAF8F1
     الحل: استخدم cssVars من @/styles/theme بدلاً من ذلك
     الكود: backgroundColor: '#FAF8F1',

  2. السطر 78:8
     القاعدة: Rule 2: Internationalization
     المشكلة: نص hardcoded في JSX: "Welcome to our platform"
     الحل: استخدم useTranslations من next-intl
     الكود: <h1>Welcome to our platform</h1>

⚠️  التحذيرات:

  1. السطر 15:5
     القاعدة: Rule 7: RTL Support
     المشكلة: استخدام directional class: ml-
     الحل: استخدم logical properties: ps/pe/start/end بدلاً من left/right
     الكود: className="ml-4 flex items-center"
```

## 🔧 التكامل مع سير العمل

### في CI/CD Pipeline

أضف السكريبت إلى pipeline الخاص بك:

```yaml
- name: Validate Design Rules
  run: npm run validate:design src/
```

### في Pre-commit Hook

أضف إلى `.husky/pre-commit`:

```bash
npm run validate:design $(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(tsx|ts)$')
```

### في VS Code Tasks

أضف إلى `.vscode/tasks.json`:

```json
{
  "label": "Validate Design Rules",
  "type": "shell",
  "command": "npm run validate:design ${file}",
  "problemMatcher": []
}
```

## 📋 الأخطاء الشائعة والحلول

### ❌ خطأ: استخدام لون hex مباشر

```tsx
// ❌ خطأ
<div style={{ backgroundColor: '#FAF8F1' }}>

// ✅ صحيح
import { cssVars } from '@/styles/theme';
<div style={{ backgroundColor: cssVars.neutral.bg }}>
```

### ❌ خطأ: نص hardcoded

```tsx
// ❌ خطأ
<h1>مرحباً بك في المنصة</h1>;

// ✅ صحيح
import { useTranslations } from 'next-intl';

const t = useTranslations('home');
<h1>{t('welcomeTitle')}</h1>;
```

### ❌ خطأ: استخدام colors بدلاً من cssVars

```tsx
// ❌ خطأ
import { colors } from '@/styles/theme';
<div style={{ backgroundColor: colors.neutral.bg }}>

// ✅ صحيح
import { cssVars } from '@/styles/theme';
<div style={{ backgroundColor: cssVars.neutral.bg }}>
```

### ❌ خطأ: استخدام directional classes

```tsx
// ❌ خطأ
<div className="ml-4 mr-2">

// ✅ صحيح
<div className="ms-4 me-2">
```

## 💡 نصائح

1. **قم بفحص الملفات قبل الـ commit**

   ```bash
   npm run validate:design src/components/features/home/NewComponent.tsx
   ```

2. **استخدم JSON output للتحليل**

   ```bash
   npm run validate:design src/ --json report.json
   ```

3. **راجع القواعد بانتظام**
   - `frontend/eetmad/docs/design/component-building-guidelines.md`

4. **استخدم السكريبت في التطوير اليومي**
   - قبل إنشاء Pull Request
   - بعد تعديل components موجودة
   - عند إضافة features جديدة

## 🔗 روابط مفيدة

- [قواعد البناء الكاملة](../docs/design/component-building-guidelines.md)
- [نظام Theme](../src/styles/theme/)
- [ملفات الترجمة](../messages/)

## 🐛 المشاكل الشائعة

### المشكلة: السكريبت لا يجد الملفات

**الحل:**

- تأكد من المسار صحيح
- تأكد من وجود الملفات (`.tsx` أو `.ts`)
- جرب المسار الكامل

### المشكلة: كثرة False Positives

**الحل:**

- السكريبت ذكي ويتجاهل التعليقات والأمثلة
- إذا كان هناك false positive، أضف تعليق `// WRONG` أو `❌` في السطر

### المشكلة: السكريبت بطيء

**الحل:**

- فحص مجلدات محددة بدلاً من المشروع كامل
- السكريبت يتجاهل `node_modules` و `dist` تلقائياً

## 📝 ملاحظات

- السكريبت لا يعدل الملفات، فقط يفحصها
- النتائج تُصنف إلى أخطاء (errors) وتحذيرات (warnings)
- الأخطاء تؤدي إلى exit code 1
- التحذيرات تُعرض لكن لا تؤدي إلى فشل

## 🔄 التحديثات المستقبلية

- [ ] إضافة auto-fix للمشاكل البسيطة
- [ ] دعم configuration file
- [ ] إضافة قواعد مخصصة
- [ ] تحسين الأداء للمشاريع الكبيرة
- [ ] دعم incremental validation

## 📞 الدعم

إذا واجهت أي مشاكل أو كان لديك اقتراحات:

1. راجع [قواعد البناء](../docs/design/component-building-guidelines.md)
2. تحقق من الأمثلة في المشروع
3. اتصل بالفريق

---

**آخر تحديث:** 2025
**الإصدار:** 1.0.0
